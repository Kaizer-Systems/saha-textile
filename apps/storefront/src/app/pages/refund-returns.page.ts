// AnalogJS file-based route → /refund-returns. Renders the shared StaticPage,
// which derives its slug from the URL and loads assets/data/pages/refund-returns.json.

import { StaticPage } from '@features/page/static-page/static-page';

export default StaticPage;
