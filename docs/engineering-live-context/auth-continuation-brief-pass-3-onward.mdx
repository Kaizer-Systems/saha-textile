---
title: 'Authentication Continuation Brief — Pass 3 Onward'
description: 'Context-aware handoff for continuing the storefront and admin authentication build in a new session, with verified current state, remaining passes, environment specifics, and hard-won gotchas.'
audience: [backend, frontend, security, agent]
document_role: canonical
last_verified: '2026-08-11'
search_keywords: 'authentication continuation handoff pass 3 shared transport refresh single-flight rbac bootstrap oauth msg91 explain plan'
---

# Authentication continuation brief — Pass 3 onward

**Status:** Live handoff. Supersedes nothing; it sits alongside
`claude-code-authentication-api-db-build-prompt.mdx` (the execution contract) and
`authentication-use-case-delivery-and-security-matrix.mdx` (the use-case/security checklist).

**Written:** 2026-08-02, and reconciled 2026-08-11 after Auth Passes 3a–5c, 6a and the
admin browser E2E completed on branch `feat/api-db-env-secrets-sync`.

**Progress authority:** `project-progress.mdx`. If this brief and that file ever disagree,
that file plus tested runtime wins.

---

## 0. Copy/paste prompt for the new session

```text
You are continuing the Saha Textile storefront/admin authentication build. Act as a
principal software architect, senior NestJS/Angular engineer, MongoDB performance engineer,
application-security engineer and penetration tester.

Read these completely before changing anything — not headings, the whole file:

1. `AGENTS.md`
2. `docs/engineering-live-context/auth-continuation-brief-pass-3-onward.mdx`  (this brief)
3. `docs/engineering-live-context/project-progress.mdx`
4. `docs/engineering-live-context/owner-decisions-log.mdx`
5. `docs/engineering-live-context/authentication-use-case-delivery-and-security-matrix.mdx`
6. `docs/engineering-live-context/claude-code-authentication-api-db-build-prompt.mdx`
7. `docs/engineering-live-context/codex-auth-architecture-db-and-request-plan.mdx`
8. `docs/engineering-live-context/api-db-development-roadmap-with-pending-decision-gates.mdx`
9. `docs/engineering-live-context/environment-variables.mdx`
10. `docs/engineering-live-context/pending-decisions.mdx`

Then inspect actual source, tests and `git status --short`. Tested runtime and current
source override stale prose; owner decisions override older architecture prose.

## Operating rules for this engagement — the owner set these explicitly

1. **EXTREMELY SMALL PASSES.** One coherent change per pass. A pass that touches contracts
   AND persistence AND the API AND the client is too big — split it.
2. **ASK BEFORE EVERY PASS.** Use the question tool. Present 2-4 concrete options for what
   to do next and mark one `(Recommended)` as the FIRST option, with the reason. Do not
   start implementing until the owner answers. This applies to every pass, not just the
   first.
3. **After each completed pass:** run the gates in §6 of the brief, then append a dated,
   accurate entry to `project-progress.mdx`. Use `[DONE]`/`[PARTIAL]`/`[BLOCKED]`/
   `[REVERTED]`/`[N/A]` honestly. Record commands actually run with real counts.
4. **Do NOT touch developer-portal derived pages** (`docs/admin/**`, `docs/storefront/**`,
   `docs/backend/**`, `docs/database/**`, `docs/architecture/**`, `docs/business-flows/**`,
   `docs/frontend/**`, `docs/getting-started/**`, `docs/tooling/**`, `docs/maintenance/**`,
   `docs/troubleshooting/**`, `docs/api/**`, `docs/decisions/**`, `docs/_data/**`). A
   separate tool reconciles those. Do not run or chase the portal validator; it WILL report
   provenance staleness on those pages and that is expected, not a regression.
   `docs/engineering-live-context/**` IS yours to update.
5. **Never commit, stage or push** unless the owner separately asks. Check
   `git diff --cached --name-only` is empty before finishing. If a tool stages something as
   a side effect, `git reset` it.
6. **Preserve all existing owner and tool changes.** Inspect the working tree at the start
   of every pass and never revert work outside the selected scope.
7. Do not claim a check ran unless it ran and you captured the real output. Do not fabricate
   provider credentials, provider responses or E2E results.
8. If you find a defect while testing, fix it or report it — do not leave dead client-side
   code that cannot work (this already happened once; see §5 "PIN sub-code").

Report at the start: current branch, `git status --short`, what you verified about the state
in §2 of the brief, and your proposed next pass with options.
```

---

## 1. Where the work sits

Branch `feat/api-db-env-secrets-sync`. Inspect `git status` and recent commit bodies before
each pass; this brief does not assert that the working tree is clean or that a specific commit
is current.

| Chunk / pass                                | State                                                                                                     |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| D1 auth persistence                         | **DONE**, and physical collection names now reconciled (Auth Pass 1)                                      |
| D2 session lifecycle                        | **DONE**                                                                                                  |
| D3 storefront endpoints                     | **PARTIAL** — OAuth, session list/revoke, step-up, email change, guest merge, pending intents missing     |
| D4 admin auth                               | **PARTIAL** — bootstrap/RBAC APIs done; Security Settings, browser PIN proof and idle-lock client missing |
| D5 RBAC / privacy / ownership               | **PARTIAL** — registry/assignments/admin authority and BOLA done; guest merge/pending intents missing     |
| Auth Pass 1 — collection names              | **DONE**                                                                                                  |
| Auth Pass 2 — T0 client reconciliation      | **DONE**                                                                                                  |
| Auth Passes 2a–2f — admin password recovery | **DONE**                                                                                                  |

---

## 2. What is actually true now (verify before trusting)

### Persistence

- All **34** Mongoose schemas declare an explicit `collection` name; 31 names match the ratified
  64-node Schema Nebula graph. `packages/adapters-db-mongo/src/collection-names.ts` is the
  single declaration; `test/collection-names.test.ts` asserts every model against it and
  reads `docs/_data/instruments/schema-nebula.json` directly.
- `pnpm mongo:align-collections [-- --dry-run]` migrates a legacy database. Idempotent,
  refuses to merge when both names hold data, no `dropTarget`, never runs at boot.
- `authRateLimits` is intentionally outside the 64-node graph. `productQuestions` and
  `ratingAggregates` are implemented models with **no node in the graph** — an open
  documentation gap, not a licence to rename them.

### API

- Cookie-only sessions. `SessionGuard` is global; routes opt out with `@Public()`. It reads
  the access token from the httpOnly cookie ONLY — bearer is ignored everywhere.
- `CsrfGuard` is global and fails closed on unsafe methods when session cookies are present.
- `@Audience('storefront'|'admin')` enforces surface isolation.
- Admin password recovery is complete: `POST /auth/admin/password/forgot` and
  `POST /auth/admin/password/reset`, audience-bound, with `users.pinRevalidationRequiredAt`
  suspending PIN use until a successful password login (cleared inside
  `recordSuccessfulLogin`, so no caller can skip it).
- A closed 33-code registry, active assignment resolution and deny-by-default
  `/admin/roles`, `/admin/permissions` and `/admin/users/**` routes are implemented.
  No-delegation-above-self, last-administrator protection, offboarding/session revocation,
  audit evidence and permission-version invalidation are negative-tested.
- First-administrator bootstrap is an operator-only CLI with no HTTP route. It generates the
  password, stores only its Argon2 hash, writes a critical actorless audit record and refuses
  once any administrative authority exists.

### Angular clients

- **No fake token, no bearer header, no session in browser storage, no demo credentials, in
  either app.** Enforced mechanically by `scripts/check-browser-auth.mjs` in `pnpm lint`.
- Each app owns one typed auth gateway (`core/auth/auth-gateway.ts` abstract class as DI
  token) and exactly one HTTP adapter (`core/auth/http-auth.gateway.ts`) holding route
  paths. Components, guards and stores depend on the gateway, never `HttpClient`.
- Stores are session facades with a three-valued status: `unknown` / `anonymous` /
  `authenticated`. `unknown` must not be collapsed into `anonymous` — that bounces a
  signed-in user to login during hydration.
- Session resolves before routing via an app initializer that awaits `runtimeConfigReady`
  (Angular starts initializers concurrently; registration order is NOT ordering).
- Storefront bootstrap is browser-only by design: SSR has no cookie jar and must not render
  a per-user account shell.

### Test counts at handoff

`contracts 160` · `core-domain 98` · `api 131` · `http-transport 157` · `admin 61` ·
`storefront 57` · `adapters-db-mongo 103` (4 source-only + 99 DB-gated).

---

## 3. Pass status and remaining work — dependency order

Each is sized to be one small pass. Confirm the choice with the owner before starting.

| #      | Pass                                                                                                   | Notes                                                                                                              |
| ------ | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------ |
| **3a** | Shared framework-free transport policy package                                                         | **DONE** — CSRF, refresh coordination, refusal and request policy shared by both clients.                          |
| **3b** | Consume the shared transport from both Angular applications                                            | **DONE** — duplicated transport primitives retired.                                                                |
| **3c** | Refresh single-flight + 401 recovery                                                                   | **DONE** — stable refusal mapping and cross-tab exclusion are implemented and tested.                              |
| **3d** | Consume selected shared contracts in both client gateways                                              | **DONE** — contract alignment is mechanically checked without shipping server-only secrets.                        |
| **4**  | Route/contract categorization, legacy-guard retirement, stable OpenAPI ids/tags and refusal reasons    | **DONE** — bearer guards retired and 59 operations have explicit stable ids.                                       |
| **5a** | Canonical permission registry with stable codes                                                        | **DONE** — 33 codes, typed route declarations and a justified server-only subset.                                  |
| **5b** | `roles` + `userRoleAssignments` collections, repositories and unique active-assignment index           | **DONE** — explicit physical names and rs0 proof.                                                                  |
| **5c** | `/admin/roles/**`, `/admin/permissions/**`, `/admin/users/**` plus escalation/BOLA/TOCTOU tests        | **DONE** — effective assignments, deny-by-default, no-delegation, last-admin/offboarding and direct-API negatives. |
| **6a** | First-administrator bootstrap — operator-only, audited, no public route, impossible to re-run          | **DONE** — CLI and real admin browser sign-in/authority proof.                                                     |
| **6b** | Admin Security Settings (recent-password proof) + PIN configure/change                                 | Partially exists (`POST /auth/admin/pin`); needs the settings surface.                                             |
| **6c** | Admin 15-minute idle soft lock + PIN quick-resume UX                                                   | `POST /auth/admin/resume` exists server-side; the client overlay does not. Must never queue mutations.             |
| **7**  | Split `authIdentities` / `passwordCredentials` / `pinCredentials` out of the embedded user document    | Locked target model; currently embedded on `users`. Migration + tests.                                             |
| **8**  | Google + Meta verifier adapters with fixture/contract tests                                            | `OAuthStateRepository` and the collection already exist. Real E2E credential-gated.                                |
| **9**  | MSG91 notification adapter with fixture tests                                                          | Console adapter is the current `NotificationPort`. Real delivery credential-gated.                                 |
| **10** | Scale fixtures (1,000 products / 10,000 orders / multi-year audit) + `explain('executionStats')` suite | Must prove intended index use, not `COLLSCAN`, at seeded scale.                                                    |
| **11** | Security-negative campaign + OpenAPI/KB reconciliation + final verification                            | The matrix's negative-test list is the checklist.                                                                  |

### Carried debt that is not a pass of its own

- **PIN refusal sub-code.** `HttpExceptionFilter` replaces every `401` with the generic
  `unauthorized` envelope, so the admin UI cannot distinguish wrong PIN / brute-force lock /
  post-reset suspension. Needs a stable machine-readable sub-code. Fold into pass 4 or 6.
- **Admin browser E2E** is complete for password login, cookie-only reload, expiry recovery,
  role mutation, permission invalidation, last-admin refusal and logout. PIN login remains
  intentionally owed to 6b, and idle lock remains 6c.
- **Storefront global error interceptor** logs the expected anonymous `/me` 401 as an error.
  Cosmetic noise; fold into 3c.
- **Guest→user cart merge, pending intents, checkout idempotency** are Chunk G, not auth.
- **Schema Nebula reconciliation is complete.** It remains 64 nodes / 31 graph-backed
  current / 33 target; `authRateLimits`, `productQuestions` and `ratingAggregates` stay
  current catalogue evidence outside the fixed graph.

---

## 4. Environment specifics

- **Mongo:** Docker `saha-textile-mongo`, MongoDB 8.3, single-node `rs0`, published
  loopback-only on **27018** on this machine (`docker/mongo/.env` → `MONGO_HOST_PORT=27018`,
  mirrored as `MONGODB_PORT` in gitignored `apps/api/.env`).
- **Effective Compass / tooling URI:**
  `mongodb://127.0.0.1:27018/saha_textile_local?replicaSet=rs0&directConnection=true`
  Always re-derive it with `pnpm mongo:status` rather than assuming.
- Canonical local ports: storefront `4200`, admin `4300`, API `4000`.
- `NOTIFICATION_PROVIDER=console` locally. The console adapter logs only a hashed
  destination and withholds the payload — OTP codes and tokens never reach a log.

---

## 5. Gotchas that will otherwise cost you an hour each

1. **`pnpm validate` does not exist at root.** §13 of the build prompt names it; the real
   gates are in §6 below.
2. **Turbo caches tests.** Use `pnpm turbo run test --force` for a real run.
3. **Adapter integration tests are opt-in:**
   `MONGODB_PORT=27018 RUN_DB_IT=1 pnpm --filter @saha-textile/adapters-db-mongo test`.
   Without those vars the 99 rs0 tests skip; four source-only collection checks still run.
4. **`apps/api` `pnpm start` runs `node dist/main.js`.** Always `pnpm --filter
@saha-textile/api build` before probing, or you will measure a stale `dist`.
5. **Angular apps have no Node types, correctly.** A test needing `node:fs` cannot live in
   `apps/*/src/**/*.spec.ts` — the Analog Vite plugin fails to transform it. Repo-policy
   scanners belong in `scripts/*.mjs` wired into `pnpm lint`, like `check-browser-auth.mjs`.
6. **`import.meta` is illegal in `adapters-db-mongo`'s CommonJS output.** Resolve paths from
   `process.cwd()` with an assertion instead.
7. **Never `JSON.parse`/`stringify` an i18n file.** It reorders numeric-like keys and
   retabs the whole file. Append entries textually; validate by parsing afterwards.
8. **CSRF on probes:** an unsafe request carrying session cookies needs the `x-csrf-token`
   header matching the readable `st_csrf` cookie, or you get `403` and will misread it as an
   auth failure. Read it with `awk '/st_csrf/{print $7}' cookiejar.txt`.
9. **Session 401s can carry a stable refusal reason.** Clients may recover only the documented
   session states; credential failures such as a wrong PIN remain generic and must not become
   an enumeration channel.
10. **Use the first-admin operator CLI, never direct database inserts.** Run
    `pnpm --filter @saha-textile/api bootstrap:first-admin -- --email <approved-address>`
    from a trusted operator shell. It prints the generated password once and refuses when an
    administrator already exists; do not capture the password in documentation, logs or source.
11. **`rm -rf` is blocked by the permission classifier.** Use `git rm`, then `git reset` to
    unstage — `git rm` stages the deletion, and staging is not allowed here.
12. **`check-browser-auth.mjs` strips comments before matching**, so documenting a forbidden
    pattern is safe. `browser-auth:allow` on a line is the deliberate opt-out.
13. **The Browser pane can stop compositing frames** mid-session and not recover. Fall back
    to a built-bundle scan (`grep -rlF` over `apps/*/dist/**/*.js`) plus unit tests, and say
    plainly in the progress entry that browser E2E did not run.
14. **Matrix §1 is verbatim approved text.** Record implementation reconciliations in §2
    (the addendum), never by editing those tables. §2.5 is the precedent.

---

## 6. Verification gates — run after every pass

```bash
pnpm typecheck
pnpm lint            # includes check:naming and check:browser-auth
pnpm format:check
pnpm turbo run test --force
git diff --check
```

Plus, when the pass touches persistence:

```bash
MONGODB_PORT=27018 RUN_DB_IT=1 pnpm --filter @saha-textile/adapters-db-mongo test
```

Plus, when the pass touches an Angular app:

```bash
pnpm --filter @saha-textile/admin build
pnpm --filter @saha-textile/storefront build
```

Do **not** run the developer-portal validator or build — portal reconciliation is out of
scope for this engagement.

Finish with `git diff --cached --name-only` returning nothing.

---

## 7. Non-negotiables carried forward

- Hexagonal layering. Core imports no NestJS/Fastify/Mongoose/provider SDK. Adapters at the
  edges. Angular calls the API only.
- One HTTP auth adapter per audience; storefront code may not name an admin route and vice
  versa — enforced by `check-browser-auth.mjs`.
- No auth token in `localStorage`, `sessionStorage`, IndexedDB, URLs, analytics, logs, NgRx
  persisted state, service-worker cache or SSR response cache.
- Anti-enumeration: login, registration collision, OTP request and recovery answer
  identically for known and unknown identifiers. Never branch UI or routing on the outcome.
- Admin launch login methods are exactly: email-or-username + password; email-or-username +
  configured six-digit PIN; PIN quick-resume for an already-open session. No social login,
  no self-registration, no OTP login. The PIN is never described as MFA.
- Object-level authorization on every owned resource; `assertOwnership` throws **404**, not
  403, so an id cannot be probed for existence.
- Every admin mutation is audited; audit rows commit in the same transaction as the mutation.
- Never log or audit passwords, PINs, OTPs, cookies, tokens or provider payloads.
