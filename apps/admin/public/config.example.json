{
	"_readme": "Public (non-secret) runtime config, fetched at app init by src/app/core/config/runtime-config.ts. The committed config.json holds LOCALHOST defaults only. Staging/prod values are NEVER committed — the deploy pipeline writes/mounts config.json into the container (roadmap §0d render-frontend-config). No secrets ever go in this file.",
	"apiUrl": "https://localhost:4000",
	"adminUrl": "https://localhost:4300"
}
